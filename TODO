MACOS silicon.h:

- make sure DND works
- RGFW_windowAttribsChange support
- fix multi-window bugs

- version control (EGL)
- fix macOS DND string bug (string has extra data at the end)

(addons)
    - init drag function (drag n' drop) (output)
    
(multiplatform support)
    - controller support (windows) (macos)

(testing)
    - test EGL and OSmesa for (windows) (macos)

(upkeepy)
    - check for memory leaks (after updates are done) (mostly done)
    - Document source code
