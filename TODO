(addons)
    - controller support (windows) (macos)
    - init drag function (drag n' drop)
    - global key system
    - custom cursors

(testing)
    - test hide mouse function (windows) 
    - test EGL and OSmesa for (windows)

(macos)
    - transparent window
    - on focus
    - drag and drop (input)
    - set icon
    - clipboard I/O
    - EGL
    - OS Mesa
    - error handling

(other)
    - opengl vbo wrangler for windows (maybe as a seperate [small/single header] library)

(upkeepy)
    - made error messages for possible errors

    - check for memory leaks (after updates are done)
    
    - Document source code