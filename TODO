(addons)
    - init drag function (drag n' drop) (output)
    
(multiplatform support)
    - controller support (windows) (macos)
    - custom cursors (windows) (macos)

(testing)
    - test EGL and OSmesa for (windows) (macos)

(macos)
    - get transparent window to work
    - set icon (runtime function)

(upkeepy)
    - made error messages for possible errors
    - check for memory leaks (after updates are done)
    - Document source code